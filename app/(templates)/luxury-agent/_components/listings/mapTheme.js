/**
 * Luxury Agent map palette — dark basemap, gold markers.
 * Consumed by the shared MapView, which is deliberately brand-free.
 */
export const MAP_THEME = {
  mapStyle:         'mapbox://styles/mapbox/dark-v11',
  markerBg:         '#C9A96E',
  markerText:       '#0A0A0A',
  popupBg:          '#0D0D0D',
  popupText:        '#FFFFFF',
  popupMuted:       'rgba(255,255,255,0.5)',
  placeholderClass: 'bg-[#1A1A1A] text-white/40',
}
